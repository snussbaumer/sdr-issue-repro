spring:
  application:
    name: sdr-issue-repro
  datasource:
    url: jdbc:h2:mem
    email: SA
    password:
  jpa:
    show_sql: true
    hibernate:
      ddl-auto: none
    properties:
      hibernate:
        dialect: org.hibernate.dialect.H2Dialect
